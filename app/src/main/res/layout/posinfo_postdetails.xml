<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="12dp"
    android:orientation="vertical">

    <TextView
        android:id="@id/title_postdetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:ellipsize="end"
        android:maxLines="2"
        android:text="Title"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

    <TextView
        android:id="@id/from_postdetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_postdetails"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="@string/homepage_post"
        android:textColor="@android:color/holo_blue_dark"
        android:textSize="@dimen/textsize12sp" />

    <TextView
        android:id="@id/time_postdetails"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@id/from_postdetails"
        android:text="时间戳"
        android:textColor="@android:color/holo_blue_dark"
        android:textSize="@dimen/textsize12sp" />
</RelativeLayout>